# QuestBoard backend

FastAPI backend for QuestBoard.

Initial responsibilities:

- expose a healthcheck;
- manage tasks, projects and local calendar items;
- calculate XP / gold rewards;
- persist data in SQLite;
- expose RPG entities such as company, adventurer, camp upgrade and expedition.

The backend is intentionally local-first.
